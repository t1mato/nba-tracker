# The charts' colours are validated against a light surface, so the app commits
# to a light theme rather than flipping palettes.
[theme]
base = "light"
backgroundColor = "#f9f9f7"
secondaryBackgroundColor = "#fcfcfb"
textColor = "#0b0b0b"
primaryColor = "#2a78d6"

[server]
headless = true
